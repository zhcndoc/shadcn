---
title: 日期选择器 Date Picker
description: 带有范围和预设选项的日期选择器组件。
component: true
---

<ComponentPreview
  name="calendar-22"
  title="出生日期选择器"
  description="带有出生日期选择功能的日历。"
/>

## 安装

日期选择器是通过组合 `<Popover />` 和 `<Calendar />` 组件构建的。

请参阅 [Popover](/docs/components/popover#installation) 和 [Calendar](/docs/components/calendar#installation) 组件的安装说明。

## 用法

```tsx showLineNumbers title="components/example-date-picker.tsx"
"use client"

import * as React from "react"
import { format } from "date-fns"
import { Calendar as CalendarIcon } from "lucide-react"

import { cn } from "@/lib/utils"
import { Button } from "@/components/ui/button"
import { Calendar } from "@/components/ui/calendar"
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover"

export function DatePickerDemo() {
  const [date, setDate] = React.useState<Date>()

  return (
    <Popover>
      <PopoverTrigger asChild>
        <Button
          variant="outline"
          data-empty={!date}
          className="data-[empty=true]:text-muted-foreground w-[280px] justify-start text-left font-normal"
        >
          <CalendarIcon />
          {date ? format(date, "PPP") : <span>请选择日期</span>}
        </Button>
      </PopoverTrigger>
      <PopoverContent className="w-auto p-0">
        <Calendar mode="single" selected={date} onSelect={setDate} />
      </PopoverContent>
    </Popover>
  )
}
```

更多信息请参阅 [React DayPicker](https://react-day-picker.js.org) 文档。

## 示例

### 出生日期选择器

<ComponentPreview
  name="calendar-22"
  title="出生日期选择器"
  description="带有出生日期选择功能的日历。"
/>

### 带输入框的选择器

<ComponentPreview
  name="calendar-28"
  title="带输入框的选择器"
  description="带输入框和选择器的日历。"
/>

### 日期和时间选择器

<ComponentPreview
  name="calendar-24"
  title="日期和时间选择器"
  description="带有日期和时间选择功能的日历。"
/>

### 自然语言选择器

该组件使用 `chrono-node` 库解析自然语言日期。

<ComponentPreview
  name="calendar-29"
  title="自然语言选择器"
  description="带有自然语言选择功能的日历。"
/>
