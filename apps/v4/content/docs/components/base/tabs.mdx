---
title: 标签页
description: 一组分层内容区域——称为标签面板——一次只显示一个。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/tabs
  api: https://base-ui.com/react/components/tabs#api-reference
---

<ComponentPreview
  styleName="base-nova"
  name="tabs-demo"
  previewClassName="h-96"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add tabs
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>安装以下依赖：</Step>

```bash
npm install @base-ui/react
```

<Step>将以下代码复制并粘贴到您的项目中。</Step>

<ComponentSource
  name="tabs"
  title="components/ui/tabs.tsx"
  styleName="base-nova"
/>

<Step>根据您的项目设置更新导入路径。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx showLineNumbers
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
```

```tsx showLineNumbers
<Tabs defaultValue="account" className="w-[400px]">
  <TabsList>
    <TabsTrigger value="account">账户</TabsTrigger>
    <TabsTrigger value="password">密码</TabsTrigger>
  </TabsList>
  <TabsContent value="account">在此处更改您的账户信息。</TabsContent>
  <TabsContent value="password">在此处更改您的密码。</TabsContent>
</Tabs>
```

## 示例

### 线形

在 `TabsList` 上使用 `variant="line"` 属性以获得线形样式。

<ComponentPreview styleName="base-nova" name="tabs-line" />

### 垂直

使用 `orientation="vertical"` 实现垂直标签页。

<ComponentPreview styleName="base-nova" name="tabs-vertical" />

### 禁用

<ComponentPreview styleName="base-nova" name="tabs-disabled" />

### 图标

<ComponentPreview styleName="base-nova" name="tabs-icons" />

## API 参考

请参阅 [Base UI 标签页](https://base-ui.com/react/components/tabs#api-reference) 文档。