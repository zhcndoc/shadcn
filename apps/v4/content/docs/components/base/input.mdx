---
title: 输入框
description: 一个用于表单和用户数据输入的文本输入组件，内置样式和无障碍特性。
base: base
component: true
---

<ComponentPreview
  styleName="base-nova"
  name="input-demo"
  previewClassName="*:max-w-xs"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>将以下代码复制并粘贴到您的项目中。</Step>

<ComponentSource
  name="input"
  title="components/ui/input.tsx"
  styleName="base-nova"
/>

<Step>更新导入路径以匹配您的项目设置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用

```tsx
import { Input } from "@/components/ui/input"
```

```tsx
<Input />
```

## 示例

### 基础

<ComponentPreview
  styleName="base-nova"
  name="input-basic"
  previewClassName="*:max-w-xs"
/>

### 字段

使用 `Field`、`FieldLabel` 和 `FieldDescription` 创建带标签和描述的输入框。

<ComponentPreview
  styleName="base-nova"
  name="input-field"
  previewClassName="*:max-w-xs"
/>

### 字段组

使用 `FieldGroup` 展示多个 `Field` 块，构建表单。

<ComponentPreview
  styleName="base-nova"
  name="input-fieldgroup"
  previewClassName="*:max-w-xs"
/>

### 禁用

使用 `disabled` 属性禁用输入框。要为禁用状态添加样式，请给 `Field` 组件添加 `data-disabled` 属性。

<ComponentPreview
  styleName="base-nova"
  name="input-disabled"
  previewClassName="*:max-w-xs"
/>

### 无效

使用 `aria-invalid` 属性标记输入框为无效状态。要为无效状态添加样式，请给 `Field` 组件添加 `data-invalid` 属性。

<ComponentPreview
  styleName="base-nova"
  name="input-invalid"
  previewClassName="*:max-w-xs"
/>

### 文件

使用 `type="file"` 属性创建文件输入框。

<ComponentPreview
  styleName="base-nova"
  name="input-file"
  previewClassName="*:max-w-xs"
/>

### 内联

使用带有 `orientation="horizontal"` 的 `Field` 创建内联输入框。
配合 `Button` 创建带按钮的搜索输入框。

<ComponentPreview
  styleName="base-nova"
  name="input-inline"
  previewClassName="*:max-w-xs"
/>

### 网格

使用网格布局将多个输入框并排放置。

<ComponentPreview
  styleName="base-nova"
  name="input-grid"
  previewClassName="p-6"
/>

### 必填

使用 `required` 属性标明必填输入框。

<ComponentPreview
  styleName="base-nova"
  name="input-required"
  previewClassName="*:max-w-xs"
/>

### 标签徽章

在标签中使用 `Badge` 突出显示推荐字段。

<ComponentPreview
  styleName="base-nova"
  name="input-badge"
  previewClassName="*:max-w-xs"
/>

### 输入组

要在输入框内添加图标、文字或按钮，请使用 `InputGroup` 组件。更多示例请参见 [输入组](/docs/components/input-group) 组件。

<ComponentPreview
  styleName="base-nova"
  name="input-input-group"
  previewClassName="*:max-w-xs"
/>

### 按钮组

要向输入框添加按钮，请使用 `ButtonGroup` 组件。更多示例请参见 [按钮组](/docs/components/button-group) 组件。

<ComponentPreview
  styleName="base-nova"
  name="input-button-group"
  previewClassName="*:max-w-xs"
/>

### 表单

一个包含多个输入框、选择框和按钮的完整表单示例。

<ComponentPreview
  styleName="base-nova"
  name="input-form"
  previewClassName="h-[32rem]"
/>

## RTL

要在 shadcn/ui 中启用 RTL 支持，请参阅 [RTL 配置指南](/docs/rtl)。

<ComponentPreview
  styleName="base-nova"
  name="input-rtl"
  direction="rtl"
  previewClassName="*:max-w-xs"
/>