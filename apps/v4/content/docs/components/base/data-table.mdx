---
title: 数据表格
description: 使用 TanStack Table 构建的强大表格和数据网格。
base: base
component: true
links:
  doc: https://tanstack.com/table/latest/docs/overview
---

<ComponentPreview
  styleName="base-nova"
  name="data-table-demo"
  align="start"
  previewClassName="items-start h-auto px-4 md:px-8"
  hideCode
/>

## 介绍

我创建的每个数据表或数据网格都是独一无二的。它们的行为都不同，具有特定的排序和筛选要求，并且使用不同的数据源。

将所有这些变化整合到单个组件中是没有意义的。这样做会失去 [headless UI](https://tanstack.com/table/latest/docs/overview#what-is-headless-ui) 所提供的灵活性。

所以，与其提供一个数据表组件，我认为提供一个如何构建自己的数据表的指南更加有帮助。

我们将从基础的 `<Table />` 组件开始，手把手构建一个复杂的数据表。

<Callout className="mt-4">

**小贴士：** 如果你发现自己在应用中的多个地方都使用同一个表格，你可以随时将其提取成一个可复用的组件。

</Callout>

## 目录

本指南将向你展示如何使用 [TanStack Table](https://tanstack.com/table) 和 `<Table />` 组件来构建你自己的定制数据表。我们将涵盖以下主题：

- [设置表格功能](#set-up-table-features)
- [基础表格](#basic-table)
- [行操作](#row-actions)
- [分页](#pagination)
- [排序](#sorting)
- [筛选](#filtering)
- [可见性](#visibility)
- [行选择](#row-selection)
- [可复用组件](#reusable-components)

## 安装

1. 将 `<Table />` 组件添加到你的项目中：

```bash
npx shadcn@latest add table
```

2. 添加 `@tanstack/react-table` 依赖。本指南使用 **TanStack Table v9**：

```bash
npm install @tanstack/react-table
```

## 前提条件

我们将构建一个展示最近付款记录的表格。我们的数据结构如下：

```tsx showLineNumbers
type Payment = {
  id: string
  amount: number
  status: "pending" | "processing" | "success" | "failed"
  email: string
}

export const payments: Payment[] = [
  {
    id: "728ed52f",
    amount: 100,
    status: "pending",
    email: "m@example.com",
  },
  {
    id: "489e1d42",
    amount: 125,
    status: "processing",
    email: "example@gmail.com",
  },
  // ...
]
```

## 项目结构

首先创建以下文件结构：

```txt
app
└── payments
    ├── columns.tsx
    ├── data-table-features.ts
    ├── data-table.tsx
    └── page.tsx
```

这里我使用了 Next.js 示例，但它也适用于其他任何 React 框架。

- `columns.tsx`（客户端组件）将包含我们的列定义。
- `data-table-features.ts` 将包含共享的 `features` 对象，用于告知 TanStack Table 启用哪些行为。
- `data-table.tsx`（客户端组件）将包含我们的 `<DataTable />` 组件。
- `page.tsx`（服务器组件）是我们获取数据并渲染表格的地方。

## 设置表格功能

TanStack Table v9 基于功能构建：通过使用 `tableFeatures()` 声明所需的行为——排序、过滤、分页等——即可选择启用。未列出的任何内容都会从你的构建包中进行 tree-shaking。这也包括内置的过滤和排序函数：在 `filterFns` 和 `sortFns` 下注册你的列所依赖的函数（我们的电子邮件过滤器使用 `includesString`，字符串列使用 `text`／`alphanumeric` 进行排序）。

```tsx showLineNumbers title="app/payments/data-table-features.ts"
import {
  columnFilteringFeature,
  columnVisibilityFeature,
  createFilteredRowModel,
  createPaginatedRowModel,
  createSortedRowModel,
  filterFn_includesString,
  rowPaginationFeature,
  rowSelectionFeature,
  rowSortingFeature,
  sortFn_alphanumeric,
  sortFn_text,
  tableFeatures,
} from "@tanstack/react-table"

// New in v9: declare the features this table uses — anything you don't
// register is tree-shaken out of the bundle.
export const features = tableFeatures({
  columnFilteringFeature,
  columnVisibilityFeature,
  rowPaginationFeature,
  rowSelectionFeature,
  rowSortingFeature,
  filteredRowModel: createFilteredRowModel(),
  paginatedRowModel: createPaginatedRowModel(),
  sortedRowModel: createSortedRowModel(),
  filterFns: { includesString: filterFn_includesString },
  sortFns: { alphanumeric: sortFn_alphanumeric, text: sortFn_text },
})

// Pass this as the first generic argument to `ColumnDef`, `Column`, `Table`,
// and `Row` so each type knows which feature APIs are available.
export type DataTableFeatures = typeof features
```

<Callout className="mt-4">

**注意：**核心行模型始终包含在内，因此你无需自行注册。可选功能的行模型通过 `create*RowModel()` 创建，并在功能对象上注册——不再有 `get*RowModel` 表格选项。

</Callout>

## 基础表格

让我们从构建一个基础表格开始。

<Steps className="mb-0 pt-2">

### 列定义

首先，定义我们的列。

```tsx showLineNumbers title="app/payments/columns.tsx" {3,5,16-17,19-29}
"use client"

import { createColumnHelper } from "@tanstack/react-table"

import { type DataTableFeatures } from "./data-table-features"

// 这个类型用于定义数据的形状。
// 如果需要，你也可以使用 Zod schema。
export type Payment = {
  id: string
  amount: number
  status: "pending" | "processing" | "success" | "failed"
  email: string
}

// Use `accessor` for data columns and `display` for columns without one.
const columnHelper = createColumnHelper<DataTableFeatures, Payment>()

export const columns = columnHelper.columns([
  columnHelper.accessor("status", {
    header: "Status",
  }),
  columnHelper.accessor("email", {
    header: "Email",
  }),
  columnHelper.accessor("amount", {
    header: "Amount",
  }),
])
```

<Callout className="mt-4">

**注意：** 列定义是你定义表格核心样式的地方。它们定义将要显示的数据，数据的格式化方式，排序和筛选行为。

</Callout>

### `<DataTable />` 组件

接下来，我们创建一个 `<DataTable />` 组件来渲染表格。

```tsx showLineNumbers title="app/payments/data-table.tsx"
"use client"

import { useTable, type ColumnDef, type RowData } from "@tanstack/react-table"

import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table"

import { features, type DataTableFeatures } from "./data-table-features"

interface DataTableProps<TData extends RowData> {
  columns: ColumnDef<DataTableFeatures, TData>[]
  data: TData[]
}

export function DataTable<TData extends RowData>({
  columns,
  data,
}: DataTableProps<TData>) {
  const table = useTable({
    features,
    data,
    columns,
  })

  return (
    <div className="overflow-hidden rounded-md border">
      <Table>
        <TableHeader>
          {table.getHeaderGroups().map((headerGroup) => (
            <TableRow key={headerGroup.id}>
              {headerGroup.headers.map((header) => {
                return (
                  <TableHead key={header.id}>
                    {header.isPlaceholder ? null : (
                      <table.FlexRender header={header} />
                    )}
                  </TableHead>
                )
              })}
            </TableRow>
          ))}
        </TableHeader>
        <TableBody>
          {table.getRowModel().rows?.length ? (
            table.getRowModel().rows.map((row) => (
              <TableRow
                key={row.id}
                data-state={row.getIsSelected() && "selected"}
              >
                {row.getVisibleCells().map((cell) => (
                  <TableCell key={cell.id}>
                    <table.FlexRender cell={cell} />
                  </TableCell>
                ))}
              </TableRow>
            ))
          ) : (
            <TableRow>
              <TableCell colSpan={columns.length} className="h-24 text-center">
                无结果。
              </TableCell>
            </TableRow>
          )}
        </TableBody>
      </Table>
    </div>
  )
}
```

<Callout>

**提示**：如果你发现自己在多个地方使用 `<DataTable />`，这就是你可以提取到 `components/ui/data-table.tsx` 以实现复用的组件。

`<DataTable columns={columns} data={data} />`

</Callout>

<Callout className="mt-4">

**`<table.FlexRender />` 与 `flexRender`：** 本指南使用 v9 的 `<table.FlexRender header={header} />` 和 `<table.FlexRender cell={cell} />` 组件，它们可以直接在表格实例上使用——无需额外导入。如果你更喜欢函数形式，或者需要在拥有 `table` 的组件之外进行渲染，也可以继续使用 v8 中经典的 `flexRender(component, context)` 辅助函数，在这种情况下你还可以导入独立的 `<FlexRender />`。

</Callout>

### 渲染表格

最后，我们在页面组件中渲染表格。

```tsx showLineNumbers title="app/payments/page.tsx" {22}
import { columns, Payment } from "./columns"
import { DataTable } from "./data-table"

async function getData(): Promise<Payment[]> {
  // 在这里从 API 获取数据。
  return [
    {
      id: "728ed52f",
      amount: 100,
      status: "pending",
      email: "m@example.com",
    },
    // ...
  ]
}

export default async function DemoPage() {
  const data = await getData()

  return (
    <div className="container mx-auto py-10">
      <DataTable columns={columns} data={data} />
    </div>
  )
}
```

</Steps>

## 单元格格式化

让我们将金额列格式化为美元金额，并且右对齐该单元格。

<Steps className="mb-0 pt-2">

### 更新列定义

更新金额的 `header` 和 `cell` 定义：

```tsx showLineNumbers title="app/payments/columns.tsx" {3-13}
export const columns = columnHelper.columns([
  columnHelper.accessor("amount", {
    header: () => <div className="text-right">Amount</div>,
    cell: ({ row }) => {
      const amount = parseFloat(row.getValue("amount"))
      const formatted = new Intl.NumberFormat("en-US", {
        style: "currency",
        currency: "USD",
      }).format(amount)

      return <div className="text-right font-medium">{formatted}</div>
    },
  }),
])
```

你也可以用同样的方法格式化其他单元格和表头。

</Steps>

## 行操作

让我们添加行操作到我们的表格中。我们将使用 `<DropdownMenu />` 组件来实现。

<Steps className="mb-0 pt-2">

### 更新列定义

更新我们的列定义以添加一个新的 `actions` 列。`actions` 单元格返回一个 `<DropdownMenu />` 组件。

```tsx showLineNumbers title="app/payments/columns.tsx" {4,6-14,18-45}
"use client"

import { createColumnHelper } from "@tanstack/react-table"
import { MoreHorizontal } from "lucide-react"

import { Button } from "@/components/ui/button"
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu"

export const columns = columnHelper.columns([
  // ...
  columnHelper.display({
    id: "actions",
    cell: ({ row }) => {
      const payment = row.original

      return (
        <DropdownMenu>
          <DropdownMenuTrigger
            render={<Button variant="ghost" className="h-8 w-8 p-0" />}
          >
            <span className="sr-only">Open menu</span>
            <MoreHorizontal className="h-4 w-4" />
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end">
            <DropdownMenuLabel>操作</DropdownMenuLabel>
            <DropdownMenuItem
              onClick={() => navigator.clipboard.writeText(payment.id)}
            >
              复制付款 ID
            </DropdownMenuItem>
            <DropdownMenuSeparator />
            <DropdownMenuItem>查看客户</DropdownMenuItem>
            <DropdownMenuItem>查看付款详情</DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      )
    },
  }),
  // ...
])
```

你可以在 `cell` 函数中通过 `row.original` 访问行数据。使用这些数据处理你的行操作，例如利用 `id` 发起 API 删除请求。

</Steps>

## 分页

接下来，我们给表格添加分页功能。

<Steps className="mb-0 pt-2">

### 分页已启用

由于我们的 features 对象包含 `rowPaginationFeature` 和 `createPaginatedRowModel()`，表格会自动将行分页为每页 10 行——无需向 `useTable` 添加任何内容。有关自定义页面大小和实现手动分页的更多信息，请参阅[分页文档](https://tanstack.com/table/latest/docs/framework/react/guide/pagination)。

### 添加分页控件

我们可以使用 `<Button />` 组件以及 `table.previousPage()` 和 `table.nextPage()` API 方法添加分页控件。

```tsx showLineNumbers title="app/payments/data-table.tsx" {1,20-37}
import { Button } from "@/components/ui/button"

export function DataTable<TData extends RowData>({
  columns,
  data,
}: DataTableProps<TData>) {
  const table = useTable({
    features,
    data,
    columns,
  })

  return (
    <div>
      <div className="overflow-hidden rounded-md border">
        <Table>
          { // .... }
        </Table>
      </div>
      <div className="flex items-center justify-end space-x-2 py-4">
        <Button
          variant="outline"
          size="sm"
          onClick={() => table.previousPage()}
          disabled={!table.getCanPreviousPage()}
        >
          上一页
        </Button>
        <Button
          variant="outline"
          size="sm"
          onClick={() => table.nextPage()}
          disabled={!table.getCanNextPage()}
        >
          下一页
        </Button>
      </div>
    </div>
  )
}
```

关于更高级的分页组件，请参阅[复用组件](#reusable-components)部分。

</Steps>

## 排序

让我们让邮箱列支持排序。

`rowSortingFeature` 和排序后的行模型已经在我们的 features 对象中注册，因此剩下的工作就是接入排序状态。

<Steps className="mb-0 pt-2">

### 更新 `<DataTable>`

```tsx showLineNumbers title="app/payments/data-table.tsx" {3,8,15,21-24}
"use client"

import * as React from "react"
import {
  useTable,
  type ColumnDef,
  type RowData,
  type SortingState,
} from "@tanstack/react-table"

export function DataTable<TData extends RowData>({
  columns,
  data,
}: DataTableProps<TData>) {
  const [sorting, setSorting] = React.useState<SortingState>([])

  const table = useTable({
    features,
    data,
    columns,
    onSortingChange: setSorting,
    state: {
      sorting,
    },
  })

  return (
    <div>
      <div className="overflow-hidden rounded-md border">
        <Table>{ ... }</Table>
      </div>
    </div>
  )
}
```

### 使表头支持排序

我们现在可以更新邮箱列的表头，添加排序控件。

```tsx showLineNumbers title="app/payments/columns.tsx" {4,8-18}
"use client"

import { createColumnHelper } from "@tanstack/react-table"
import { ArrowUpDown } from "lucide-react"

export const columns = columnHelper.columns([
  columnHelper.accessor("email", {
    header: ({ column }) => {
      return (
        <Button
          variant="ghost"
          onClick={() => column.toggleSorting(column.getIsSorted() === "asc")}
        >
          邮箱
          <ArrowUpDown className="ml-2 h-4 w-4" />
        </Button>
      )
    },
  }),
])
```

用户点击表头时，将自动实现在升序和降序之间切换的排序功能。

</Steps>

## 筛选

给表格添加一个搜索输入框，筛选邮箱列数据。

`columnFilteringFeature` 和筛选行模型已经是我们 features 对象的一部分，因此我们只需要连接筛选状态并渲染一个输入框。

<Steps className="mb-0 pt-2">

### 更新 `<DataTable>`

```tsx showLineNumbers title="app/payments/data-table.tsx" {7,13,20-22,29,32,38-46}
"use client"

import * as React from "react"
import {
  useTable,
  type ColumnDef,
  type ColumnFiltersState,
  type RowData,
  type SortingState,
} from "@tanstack/react-table"

import { Button } from "@/components/ui/button"
import { Input } from "@/components/ui/input"

export function DataTable<TData extends RowData>({
  columns,
  data,
}: DataTableProps<TData>) {
  const [sorting, setSorting] = React.useState<SortingState>([])
  const [columnFilters, setColumnFilters] = React.useState<ColumnFiltersState>(
    []
  )

  const table = useTable({
    features,
    data,
    columns,
    onSortingChange: setSorting,
    onColumnFiltersChange: setColumnFilters,
    state: {
      sorting,
      columnFilters,
    },
  })

  return (
    <div>
      <div className="flex items-center py-4">
        <Input
          placeholder="筛选邮箱..."
          value={(table.getColumn("email")?.getFilterValue() as string) ?? ""}
          onChange={(event) =>
            table.getColumn("email")?.setFilterValue(event.target.value)
          }
          className="max-w-sm"
        />
      </div>
      <div className="overflow-hidden rounded-md border">
        <Table>{ ... }</Table>
      </div>
    </div>
  )
}
```

现在已为 `email` 列启用筛选功能。你也可以为其他列添加筛选器。有关如何自定义筛选器的更多信息，请参阅[筛选文档](https://tanstack.com/table/latest/docs/framework/react/guide/column-filtering)。

</Steps>

## 显示与隐藏

使用 `@tanstack/react-table` 的可见性 API，添加列的显示与隐藏功能非常简单。

<Steps className="mb-0 pt-2">

### 更新 `<DataTable>`

```tsx showLineNumbers title="app/payments/data-table.tsx" {8,14-19,29-30,38,42,57-84}
"use client"

import * as React from "react"
import {
  useTable,
  type ColumnDef,
  type ColumnFiltersState,
  type ColumnVisibilityState,
  type RowData,
  type SortingState,
} from "@tanstack/react-table"

import { Button } from "@/components/ui/button"
import {
  DropdownMenu,
  DropdownMenuCheckboxItem,
  DropdownMenuContent,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu"

export function DataTable<TData extends RowData>({
  columns,
  data,
}: DataTableProps<TData>) {
  const [sorting, setSorting] = React.useState<SortingState>([])
  const [columnFilters, setColumnFilters] = React.useState<ColumnFiltersState>(
    []
  )
  const [columnVisibility, setColumnVisibility] =
    React.useState<ColumnVisibilityState>({})

  const table = useTable({
    features,
    data,
    columns,
    onSortingChange: setSorting,
    onColumnFiltersChange: setColumnFilters,
    onColumnVisibilityChange: setColumnVisibility,
    state: {
      sorting,
      columnFilters,
      columnVisibility,
    },
  })

  return (
    <div>
      <div className="flex items-center py-4">
        <Input
          placeholder="筛选邮箱..."
          value={table.getColumn("email")?.getFilterValue() as string}
          onChange={(event) =>
            table.getColumn("email")?.setFilterValue(event.target.value)
          }
          className="max-w-sm"
        />
        <DropdownMenu>
          <DropdownMenuTrigger render={<Button variant="outline" className="ml-auto" />}>
            Columns
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end">
            {table
              .getAllColumns()
              .filter(
                (column) => column.getCanHide()
              )
              .map((column) => {
                return (
                  <DropdownMenuCheckboxItem
                    key={column.id}
                    className="capitalize"
                    checked={column.getIsVisible()}
                    onCheckedChange={(value) =>
                      column.toggleVisibility(!!value)
                    }
                  >
                    {column.id}
                  </DropdownMenuCheckboxItem>
                )
              })}
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
      <div className="overflow-hidden rounded-md border">
        <Table>{ ... }</Table>
      </div>
    </div>
  )
}
```

这会添加一个下拉菜单，用于切换列的显示与隐藏。

</Steps>

## 行选择

接下来，我们给表格添加行选择功能。

<Steps className="mb-0 pt-2">

### 更新列定义

```tsx showLineNumbers title="app/payments/columns.tsx" {6,9-30}
"use client"

import { createColumnHelper } from "@tanstack/react-table"

import { Badge } from "@/components/ui/badge"
import { Checkbox } from "@/components/ui/checkbox"

export const columns = columnHelper.columns([
  columnHelper.display({
    id: "select",
    header: ({ table }) => (
      <Checkbox
        checked={table.getIsAllPageRowsSelected()}
        indeterminate={
          table.getIsSomePageRowsSelected() && !table.getIsAllPageRowsSelected()
        }
        onCheckedChange={(value) => table.toggleAllPageRowsSelected(!!value)}
        aria-label="全选"
      />
    ),
    cell: ({ row }) => (
      <Checkbox
        checked={row.getIsSelected()}
        onCheckedChange={(value) => row.toggleSelected(!!value)}
        aria-label="选择行"
      />
    ),
    enableSorting: false,
    enableHiding: false,
  }),
])
```

### 更新 `<DataTable>`

```tsx showLineNumbers title="app/payments/data-table.tsx" {11,20,25}
export function DataTable<TData extends RowData>({
  columns,
  data,
}: DataTableProps<TData>) {
  const [sorting, setSorting] = React.useState<SortingState>([])
  const [columnFilters, setColumnFilters] = React.useState<ColumnFiltersState>(
    []
  )
  const [columnVisibility, setColumnVisibility] =
    React.useState<ColumnVisibilityState>({})
  const [rowSelection, setRowSelection] = React.useState({})

  const table = useTable({
    features,
    data,
    columns,
    onSortingChange: setSorting,
    onColumnFiltersChange: setColumnFilters,
    onColumnVisibilityChange: setColumnVisibility,
    onRowSelectionChange: setRowSelection,
    state: {
      sorting,
      columnFilters,
      columnVisibility,
      rowSelection,
    },
  })

  return (
    <div>
      <div className="overflow-hidden rounded-md border">
        <Table />
      </div>
    </div>
  )
}
```

此操作为每一行添加了一个复选框，同时在表头添加了一个复选框以全选。

### 显示已选择的行数

你可以使用 `table.getFilteredSelectedRowModel()` API 显示已选择的行数。

```tsx
<div className="flex-1 text-sm text-muted-foreground">
  {table.getFilteredSelectedRowModel().rows.length} /{" "}
  {table.getFilteredRowModel().rows.length} 行已选择。
</div>
```

</Steps>

## 复用组件

以下是一些可用于构建数据表格的组件。它们来自 [Tasks](/examples/tasks) 演示，该演示通过 `data-table-features.ts` 模块，在每个组件之间共享其 features 对象（以及匹配的 `TasksTableFeatures` 类型）——这与我们在[设置表格功能](#set-up-table-features)中采用的模式相同。

### 列表头

使任意列的表头支持排序和隐藏功能。

<ComponentSource
  src="/app/(app)/examples/tasks/components/data-table-column-header.tsx"
  title="components/data-table-column-header.tsx"
/>

```tsx showLineNumbers {4}
export const columns = columnHelper.columns([
  columnHelper.accessor("email", {
    header: ({ column }) => (
      <DataTableColumnHeader column={column} title="邮箱" />
    ),
  }),
])
```

### 分页

为你的表格添加分页控件，包括页面大小和选择计数。

<ComponentSource
  src="/app/(app)/examples/tasks/components/data-table-pagination.tsx"
  styleName="radix-nova"
/>

```tsx
<DataTablePagination table={table} />
```

### 列切换

用于切换列显示的组件。

<ComponentSource
  src="/app/(app)/examples/tasks/components/data-table-view-options.tsx"
  styleName="radix-nova"
/>

```tsx
<DataTableViewOptions table={table} />
```

## RTL（从右到左布局）

要启用 shadcn/ui 的 RTL 支持，请参阅 [RTL 配置指南](/docs/rtl)。

<ComponentPreview
  styleName="base-nova"
  name="data-table-rtl"
  direction="rtl"
  previewClassName="items-start h-auto px-4 md:px-8"
  hideCode
/>
