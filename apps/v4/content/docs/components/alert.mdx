---
title: 警告 Alert
description: 显示用于提醒用户的提示信息。
component: true
---

<ComponentPreview
  name="alert-demo"
  title="一个带有图标、标题和描述的警报。"
  description="一个带有图标、标题和描述的警报。标题写着“注意！”，描述为“您可以使用 CLI 向应用添加组件。”"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add alert
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>将以下代码复制并粘贴到您的项目中。</Step>

<ComponentSource name="alert" title="components/ui/alert.tsx" />

<Step>更新导入路径以匹配您的项目设置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用

```tsx showLineNumbers
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"
```

```tsx showLineNumbers
<Alert variant="default | destructive">
  <Terminal />
  <AlertTitle>注意！</AlertTitle>
  <AlertDescription>
    您可以使用 CLI 向应用添加组件和依赖。
  </AlertDescription>
</Alert>
```
