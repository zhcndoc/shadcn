---
title: 标签页 Tabs
description: 一组分层内容区域——称为标签面板——一次只显示一个。
component: true
links:
  doc: https://radix.zhcndoc.com/docs/primitives/components/tabs
  api: https://radix.zhcndoc.com/docs/primitives/components/tabs#api-reference
---

<ComponentPreview
  name="tabs-demo"
  description="一个包含两种形式的标签页组件。"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令行</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add tabs
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>安装以下依赖：</Step>

```bash
npm install @radix-ui/react-tabs
```

<Step>将以下代码复制粘贴到你的项目中。</Step>

<ComponentSource name="tabs" title="components/ui/tabs.tsx" />

<Step>更新导入路径以匹配你的项目配置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用

```tsx showLineNumbers
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
```

```tsx showLineNumbers
<Tabs defaultValue="account" className="w-[400px]">
  <TabsList>
    <TabsTrigger value="account">账户</TabsTrigger>
    <TabsTrigger value="password">密码</TabsTrigger>
  </TabsList>
  <TabsContent value="account">在此处修改你的账户信息。</TabsContent>
  <TabsContent value="password">在此处更改你的密码。</TabsContent>
</Tabs>
```
