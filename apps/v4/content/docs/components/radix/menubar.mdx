---
title: 菜单栏
description: 一种常见于桌面应用程序中视觉上持久的菜单，提供快速访问一组一致命令的功能。
base: radix
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/menubar
  api: https://www.radix-ui.com/docs/primitives/components/menubar#api-reference
---

<ComponentPreview styleName="radix-nova" name="menubar-demo" />

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令行</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add menubar
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>安装以下依赖：</Step>

```bash
npm install radix-ui
```

<Step>将以下代码复制并粘贴到你的项目中。</Step>

<ComponentSource
  name="menubar"
  title="components/ui/menubar.tsx"
  styleName="radix-nova"
/>

<Step>更新导入路径以匹配你的项目配置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使用方法

```tsx showLineNumbers
import {
  Menubar,
  MenubarContent,
  MenubarGroup,
  MenubarItem,
  MenubarMenu,
  MenubarSeparator,
  MenubarShortcut,
  MenubarTrigger,
} from "@/components/ui/menubar"
```

```tsx showLineNumbers
<Menubar>
  <MenubarMenu>
    <MenubarTrigger>文件</MenubarTrigger>
    <MenubarContent>
      <MenubarGroup>
        <MenubarItem>
          新建标签页 <MenubarShortcut>⌘T</MenubarShortcut>
        </MenubarItem>
        <MenubarItem>新建窗口</MenubarItem>
      </MenubarGroup>
      <MenubarSeparator />
      <MenubarGroup>
        <MenubarItem>分享</MenubarItem>
        <MenubarItem>打印</MenubarItem>
      </MenubarGroup>
    </MenubarContent>
  </MenubarMenu>
</Menubar>
```

## 示例

### 复选框

使用 `MenubarCheckboxItem` 作为可切换选项。

<ComponentPreview styleName="radix-nova" name="menubar-checkbox" />

### 单选框

使用 `MenubarRadioGroup` 和 `MenubarRadioItem` 作为单选选项。

<ComponentPreview styleName="radix-nova" name="menubar-radio" />

### 子菜单

使用 `MenubarSub`、`MenubarSubTrigger` 和 `MenubarSubContent` 实现嵌套菜单。

<ComponentPreview styleName="radix-nova" name="menubar-submenu" />

### 带图标

<ComponentPreview styleName="radix-nova" name="menubar-icons" />

## RTL

要在 shadcn/ui 中启用 RTL（从右到左）支持，请参阅 [RTL 配置指南](/docs/rtl)。

<ComponentPreview styleName="radix-nova" name="menubar-rtl" direction="rtl" />

## API 参考

请查阅 [Radix UI 菜单栏](https://www.radix-ui.com/docs/primitives/components/menubar#api-reference) 文档。