---
title: 可调整大小  
description: 支持键盘操作的可访问可调整大小面板组及布局。  
base: radix  
component: true  
links:  
  doc: https://github.com/bvaughn/react-resizable-panels  
  api: https://github.com/bvaughn/react-resizable-panels/tree/main/packages/react-resizable-panels  
---

<ComponentPreview
  styleName="radix-nova"
  name="resizable-demo"
  previewClassName="h-80"
/>

## 关于

`Resizable` 组件基于 [react-resizable-panels](https://github.com/bvaughn/react-resizable-panels)（作者 [bvaughn](https://github.com/bvaughn)）构建。

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add resizable
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>安装以下依赖：</Step>

```bash
npm install react-resizable-panels
```

<Step>将以下代码复制粘贴到你的项目中。</Step>

<ComponentSource
  name="resizable"
  title="components/ui/resizable.tsx"
  styleName="radix-nova"
/>

<Step>根据你的项目设置更新导入路径。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx showLineNumbers
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "@/components/ui/resizable"
```

```tsx showLineNumbers
<ResizablePanelGroup orientation="horizontal">
  <ResizablePanel>一</ResizablePanel>
  <ResizableHandle />
  <ResizablePanel>二</ResizablePanel>
</ResizablePanelGroup>
```

## 示例

### 垂直方向

使用 `orientation="vertical"` 实现垂直调整大小。

<ComponentPreview styleName="radix-nova" name="resizable-vertical" />

### 拖拽手柄

在 `ResizableHandle` 上使用 `withHandle` 属性以显示可见的拖拽手柄。

<ComponentPreview styleName="radix-nova" name="resizable-handle" />

## RTL

要在 shadcn/ui 中启用 RTL（从右到左）支持，请参阅[RTL 配置指南](/docs/rtl)。

<ComponentPreview styleName="radix-nova" name="resizable-rtl" direction="rtl" />

## API 参考

请参阅 [react-resizable-panels](https://github.com/bvaughn/react-resizable-panels/tree/main/packages/react-resizable-panels) 的文档。

## 更新日志

### 2025-02-02 `react-resizable-panels` v4

更新至 `react-resizable-panels` v4。完整详情请参阅 [v4.0.0 发布说明](https://github.com/bvaughn/react-resizable-panels/releases/tag/4.0.0)。

如果你直接使用 `react-resizable-panels` 的基础组件，请注意以下变化：

| v3                           | v4                      |
| ---------------------------- | ----------------------- |
| `PanelGroup`                 | `Group`                 |
| `PanelResizeHandle`          | `Separator`             |
| `direction` 属性             | `orientation` 属性      |
| `defaultSize={50}`           | `defaultSize="50%"`     |
| `onLayout`                   | `onLayoutChange`        |
| `ImperativePanelHandle`      | `PanelImperativeHandle` |
| 面板上的 `ref` 属性          | `panelRef` 属性         |
| `data-panel-group-direction` | `aria-orientation`      |

<Callout>
  shadcn/ui 的包装组件 (`ResizablePanelGroup`, `ResizablePanel`, `ResizableHandle`) 保持不变。
</Callout>