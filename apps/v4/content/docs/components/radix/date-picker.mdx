---
title: 日期选择器 Date Picker
description: 一款带有范围和预设选项的日期选择器组件。
base: radix
component: true
---

<ComponentPreview styleName="radix-nova" name="date-picker-demo" />

## 安装

日期选择器是通过组合 `<Popover />` 和 `<Calendar />` 组件构建的。

请参阅 [Popover](/docs/components/radix/popover#installation) 和 [Calendar](/docs/components/radix/calendar#installation) 组件的安装说明。

## 用法

```tsx showLineNumbers title="components/example-date-picker.tsx"
"use client"

import * as React from "react"
import { format } from "date-fns"
import { Calendar as CalendarIcon } from "lucide-react"

import { cn } from "@/lib/utils"
import { Button } from "@/components/ui/button"
import { Calendar } from "@/components/ui/calendar"
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover"

export function DatePickerDemo() {
  const [date, setDate] = React.useState<Date>()

  return (
    <Popover>
      <PopoverTrigger asChild>
        <Button
          variant="outline"
          data-empty={!date}
          className="data-[empty=true]:text-muted-foreground w-[280px] justify-start text-left font-normal"
        >
          <CalendarIcon />
          {date ? format(date, "PPP") : <span>请选择日期</span>}
        </Button>
      </PopoverTrigger>
      <PopoverContent className="w-auto p-0">
        <Calendar mode="single" selected={date} onSelect={setDate} />
      </PopoverContent>
    </Popover>
  )
}
```

更多信息请参阅 [React DayPicker](https://react-day-picker.js.org) 文档。

## 示例

### 基本用法

一个基础的日期选择器组件。

<ComponentPreview styleName="radix-nova" name="date-picker-basic" />

### 范围选择器

一个用于选择日期范围的日期选择器组件。

<ComponentPreview styleName="radix-nova" name="date-picker-range" />

### 出生日期选择器

一个用于选择出生日期的日期选择器组件。该组件包含用于日期和月份选择的下拉标题布局。

<ComponentPreview styleName="radix-nova" name="date-picker-dob" />

### 带输入框的选择器

一个带有输入框以选择日期的日期选择器组件。

<ComponentPreview styleName="radix-nova" name="date-picker-input" />

### 时间选择器

一个带有时间输入框以选择时间的日期选择器组件。

<ComponentPreview styleName="radix-nova" name="date-picker-time" />

### 自然语言选择器

该组件使用 `chrono-node` 库解析自然语言日期。

<ComponentPreview styleName="radix-nova" name="date-picker-natural-language" />
