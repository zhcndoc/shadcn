---
title: 面包屑 Breadcrumb
description: 使用层级链接显示当前资源的路径。
component: true
---

<ComponentPreview
  name="breadcrumb-demo"
  className="[&_.preview]:p-2"
  description="带有可折叠下拉菜单的面包屑导航。"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令行</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add breadcrumb
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>复制以下代码并粘贴到你的项目中。</Step>

<ComponentSource name="breadcrumb" title="components/ui/breadcrumb.tsx" />

<Step>更新导入路径以匹配你的项目配置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx showLineNumbers
import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb"
```

```tsx showLineNumbers
<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink href="/">首页</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbLink href="/components">组件</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbPage>面包屑导航</BreadcrumbPage>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>
```

## 示例

### 自定义分隔符

为 `<BreadcrumbSeparator />` 传入自定义组件作为 `children`，即可创建自定义分隔符。

<ComponentPreview
  name="breadcrumb-separator"
  description="具有自定义分隔符的面包屑导航"
/>

```tsx showLineNumbers {1,10-12}
import { SlashIcon } from "lucide-react"

...

<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink href="/">首页</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator>
      <SlashIcon />
    </BreadcrumbSeparator>
    <BreadcrumbItem>
      <BreadcrumbLink href="/components">组件</BreadcrumbLink>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>
```

---

### 下拉菜单

你可以将 `<BreadcrumbItem />` 与 `<DropdownMenu />` 组合使用，创建面包屑中的下拉菜单。

<ComponentPreview
  name="breadcrumb-dropdown"
  className="[&_.preview]:p-2"
  description="带有下拉菜单的面包屑导航。"
/>

```tsx showLineNumbers {1-6,11-21}
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu"

...

<BreadcrumbItem>
  <DropdownMenu>
    <DropdownMenuTrigger>
      组件
    </DropdownMenuTrigger>
    <DropdownMenuContent align="start">
      <DropdownMenuItem>文档</DropdownMenuItem>
      <DropdownMenuItem>主题</DropdownMenuItem>
      <DropdownMenuItem>GitHub</DropdownMenuItem>
    </DropdownMenuContent>
  </DropdownMenu>
</BreadcrumbItem>
```

---

### 折叠显示

我们提供了 `<BreadcrumbEllipsis />` 组件，用于当面包屑太长时显示折叠状态。

<ComponentPreview
  name="breadcrumb-ellipsis"
  className="[&_.preview]:p-2"
  description="展示折叠状态的面包屑导航。"
/>

```tsx showLineNumbers {1,9}
import { BreadcrumbEllipsis } from "@/components/ui/breadcrumb"

...

<Breadcrumb>
  <BreadcrumbList>
    {/* ... */}
    <BreadcrumbItem>
      <BreadcrumbEllipsis />
    </BreadcrumbItem>
    {/* ... */}
  </BreadcrumbList>
</Breadcrumb>
```

---

### 自定义链接组件

要使用你路由库中的自定义链接组件，可以在 `<BreadcrumbLink />` 上使用 `asChild` 属性。

<ComponentPreview
  name="breadcrumb-link"
  description="使用自定义 Link 组件的面包屑导航"
/>

```tsx showLineNumbers {1,8-10}
import Link from "next/link"

...

<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink asChild>
        <Link href="/">首页</Link>
      </BreadcrumbLink>
    </BreadcrumbItem>
    {/* ... */}
  </BreadcrumbList>
</Breadcrumb>
```

---

### 响应式

下面是一个响应式的面包屑导航示例，它将 `<BreadcrumbItem />` 与 `<BreadcrumbEllipsis />`、`<DropdownMenu />` 和 `<Drawer />` 组合使用。

该示例在桌面端显示下拉菜单，在移动端显示抽屉菜单。

<ComponentPreview
  name="breadcrumb-responsive"
  className="[&_.preview]:p-2"
  description="一个响应式的面包屑导航。桌面端显示下拉菜单，移动端显示抽屉菜单。"
/>
