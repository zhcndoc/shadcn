---
title: 侧边栏 Sheet
description: 扩展 Dialog 组件以显示补充主屏幕内容的内容。
component: true
links:
  doc: https://radix.zhcndoc.com/docs/primitives/components/dialog
  api: https://radix.zhcndoc.com/docs/primitives/components/dialog#api-reference
---

<ComponentPreview name="sheet-demo" description="一个侧边栏组件。" />

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令行</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add sheet
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>安装以下依赖：</Step>

```bash
npm install @radix-ui/react-dialog
```

<Step>将以下代码复制并粘贴到您的项目中。</Step>

<ComponentSource name="sheet" title="components/ui/sheet.tsx" />

<Step>根据您的项目配置更新导入路径。</Step>

</Steps>

</TabsContent>

</CodeTabs>

### 使用方法

```tsx showLineNumbers
import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet"
```

```tsx showLineNumbers
<Sheet>
  <SheetTrigger>打开</SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>您确定要继续吗？</SheetTitle>
      <SheetDescription>
        此操作无法撤销。这将永久删除您的账户，并从我们的服务器上移除您的数据。
      </SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
```

## 示例

### 侧边位置

使用 `<SheetContent />` 的 `side` 属性来指定组件出现的屏幕边缘。可选值为 `top`、`right`、`bottom` 或 `left`。

### 大小

您可以通过 CSS 类来调整侧边栏的大小：

```tsx showLineNumbers {3}
<Sheet>
  <SheetTrigger>打开</SheetTrigger>
  <SheetContent className="w-[400px] sm:w-[540px]">
    <SheetHeader>
      <SheetTitle>您确定要继续吗？</SheetTitle>
      <SheetDescription>
        此操作无法撤销。这将永久删除您的账户，并从我们的服务器上移除您的数据。
      </SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
```
