---
title: 头像
description: 用于表示用户的图像元素，并提供备用显示内容。
base: aria
component: true
---

<ComponentPreview
  styleName="aria-nova"
  name="avatar-demo"
  previewClassName="h-72"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add avatar
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>将以下代码复制并粘贴到你的项目中。</Step>

<ComponentSource
  name="avatar"
  title="components/ui/avatar.tsx"
  styleName="aria-nova"
/>

<Step>更新导入路径以匹配你的项目设置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx showLineNumbers
import { Avatar, AvatarFallback, AvatarImage } from "@/components/ui/avatar"
```

```tsx showLineNumbers
<Avatar>
  <AvatarImage src="https://github.com/shadcn.png" />
  <AvatarFallback>CN</AvatarFallback>
</Avatar>
```

## 组合

使用以下组合构建 `Avatar`：

```text
Avatar
├── AvatarImage
├── AvatarFallback
└── AvatarBadge
```

使用以下组合构建 `AvatarGroup`：

```text
AvatarGroup
├── Avatar
│   ├── AvatarImage
│   ├── AvatarFallback
│   └── AvatarBadge
├── Avatar
│   ├── AvatarImage
│   ├── AvatarFallback
│   └── AvatarBadge
└── AvatarGroupCount
```

## 基础

带有图像和回退内容的基础头像组件。

<ComponentPreview styleName="aria-nova" name="avatar-basic" />

## 徽章

使用 `AvatarBadge` 组件向头像添加徽章。徽章位于头像的右下角。

<ComponentPreview styleName="aria-nova" name="avatar-badge" />

使用 `className` 属性为徽章添加自定义样式，例如自定义颜色、尺寸等。

```tsx showLineNumbers
<Avatar>
  <AvatarImage src="https://github.com/shadcn.png" alt="@shadcn" />
  <AvatarFallback>CN</AvatarFallback>
  <AvatarBadge className="bg-green-600 dark:bg-green-800" />
</Avatar>
```

## 带图标的徽章

你也可以在 `<AvatarBadge>` 中使用图标。

<ComponentPreview styleName="aria-nova" name="avatar-badge-icon" />

## 头像组

使用 `AvatarGroup` 组件添加一组头像。

<ComponentPreview styleName="aria-nova" name="avatar-group" />

## 头像组数量

使用 `<AvatarGroupCount>` 为头像组添加数量。

<ComponentPreview styleName="aria-nova" name="avatar-group-count" />

## 带图标的头像组

你也可以在 `<AvatarGroupCount>` 中使用图标。

<ComponentPreview styleName="aria-nova" name="avatar-group-count-icon" />

## 尺寸

使用 `size` 属性更改头像的尺寸。

<ComponentPreview styleName="aria-nova" name="avatar-size" />

## 下拉菜单

你可以使用 `Avatar` 组件作为下拉菜单的触发器。

<ComponentPreview styleName="aria-nova" name="avatar-dropdown" />

## RTL

要在 shadcn/ui 中启用 RTL 支持，请参阅 [RTL 配置指南](/docs/rtl)。

<ComponentPreview
  styleName="aria-nova"
  name="avatar-rtl"
  direction="rtl"
  previewClassName="h-72"
/>

## API 参考

### Avatar

`Avatar` 组件是包裹头像图片和备用内容的根组件。

| 属性        | 类型                        | 默认值     |
| ----------- | --------------------------- | ----------- |
| `size`      | `"default" \| "sm" \| "lg"` | `"default"` |
| `className` | `string`                    | -           |

### AvatarImage

`AvatarImage` 组件用于显示头像图片。

| 属性        | 类型     | 默认值 |
| ----------- | -------- | ------- |
| `src`       | `string` | -       |
| `alt`       | `string` | -       |
| `className` | `string` | -       |

### AvatarFallback

当图片加载失败时，`AvatarFallback` 组件用于显示备用内容。

| 属性        | 类型     | 默认值 |
| ----------- | -------- | ------- |
| `className` | `string` | -       |

### AvatarBadge

`AvatarBadge` 组件用于在头像上显示徽章指示器，通常位于右下角。

| 属性        | 类型     | 默认值 |
| ----------- | -------- | ------- |
| `className` | `string` | -       |

### AvatarGroup

`AvatarGroup` 组件用于显示一组头像，并采用重叠样式。

| 属性        | 类型     | 默认值 |
| ----------- | -------- | ------- |
| `className` | `string` | -       |

### AvatarGroupCount

`AvatarGroupCount` 组件用于在头像组中显示数量指示器，通常表示额外头像的数量。

| 属性        | 类型     | 默认值 |
| ----------- | -------- | ------- |
| `className` | `string` | -       |
