---
title: 切换组 Toggle Group
description: 一组可以切换开关状态的双状态按钮。
component: true
links:
  doc: https://radix.zhcndoc.com/docs/primitives/components/toggle-group
  api: https://radix.zhcndoc.com/docs/primitives/components/toggle-group#api-reference
---

<ComponentPreview name="toggle-group-spacing" />

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令行</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add toggle-group
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>安装以下依赖：</Step>

```bash
npm install @radix-ui/react-toggle-group
```

<Step>将以下代码复制粘贴到你的项目中。</Step>

<ComponentSource name="toggle-group" title="components/ui/toggle-group.tsx" />

<Step>更新导入路径以匹配你的项目设置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx
import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group"
```

```tsx
<ToggleGroup type="single">
  <ToggleGroupItem value="a">A</ToggleGroupItem>
  <ToggleGroupItem value="b">B</ToggleGroupItem>
  <ToggleGroupItem value="c">C</ToggleGroupItem>
</ToggleGroup>
```

## 示例

### 描边

<ComponentPreview
  name="toggle-group-outline"
  description="使用描边风格的切换组。"
/>

### 单选

<ComponentPreview
  name="toggle-group-single"
  description="一个单选切换组。"
/>

### 小号

<ComponentPreview
  name="toggle-group-sm"
  description="使用小号尺寸的切换组。"
/>

### 大号

<ComponentPreview
  name="toggle-group-lg"
  description="使用大号尺寸的切换组。"
/>

### 禁用

<ComponentPreview
  name="toggle-group-disabled"
  description="一个禁用的切换组。"
/>

### 间距

使用 `spacing={2}` 在切换组项之间添加间距。

<ComponentPreview
  name="toggle-group-spacing"
  description="A toggle group with spacing."
/>

## API 参考

### 切换组

包装切换组项的主要组件。

| Prop        | Type                        | Default     |
| ----------- | --------------------------- | ----------- |
| `type`      | `"single" \| "multiple"`    | `"single"`  |
| `variant`   | `"default" \| "outline"`    | `"default"` |
| `size`      | `"default" \| "sm" \| "lg"` | `"default"` |
| `spacing`   | `number`                    | `0`         |
| `className` | `string`                    |             |

```tsx
<ToggleGroup type="single" variant="outline" size="sm">
  <ToggleGroupItem value="a">A</ToggleGroupItem>
  <ToggleGroupItem value="b">B</ToggleGroupItem>
</ToggleGroup>
```

### 切换组项

切换组中的单个切换项。请记得为每个项添加 `aria-label` 以提高可访问性。

| Prop        | Type     | Default  |
| ----------- | -------- | -------- |
| `value`     | `string` | Required |
| `className` | `string` |          |
