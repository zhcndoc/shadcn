---
title: 输入 Input
description: 显示一个表单输入字段或看起来像输入字段的组件。
component: true
---

<ComponentPreview
  name="input-demo"
  className="[&_input]:max-w-xs"
  description="一个表单输入组件。"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">命令行</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>将以下代码复制并粘贴到你的项目中。</Step>

<ComponentSource name="input" title="components/ui/input.tsx" />

<Step>更新导入路径以匹配你的项目设置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx
import { Input } from "@/components/ui/input"
```

```tsx
<Input />
```

## 示例

### 默认

<ComponentPreview
  name="input-demo"
  className="[&_input]:max-w-xs"
  description="一个表单输入组件。"
/>

### 文件

<ComponentPreview
  name="input-file"
  className="[&_input]:max-w-xs"
  description="一个文件输入组件。"
/>

### 禁用

<ComponentPreview
  name="input-disabled"
  className="[&_input]:max-w-xs"
  description="一个禁用的输入组件。"
/>

### 带标签

<ComponentPreview
  name="input-with-label"
  className="[&_input]:max-w-xs"
  description="一个带标签的输入组件。"
/>

### 带按钮

<ComponentPreview
  name="input-with-button"
  className="[&_input]:max-w-xs"
  description="一个带按钮的输入组件。"
/>

## 更新日志

### 2025-09-18 移除 `flex` 类

编辑 `input.tsx`，从输入组件中移除 `flex` 类。此类已不再需要。
