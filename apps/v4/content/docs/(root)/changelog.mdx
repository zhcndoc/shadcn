---
title: 更新日志
description: 最新更新与公告。
toc: false
---

## 2025年10月 - 新组件

这次的新组件，我关注的是我们每天构建的那些乏味内容，那些我们反复重建的东西，并制作了可以真正复用的抽象组件。

**这些组件兼容所有组件库，Radix、Base UI、React Aria，随便什么库。复制粘贴到你的项目即可使用。**

- [Spinner](#spinner)：加载状态指示器。
- [Kbd](#kbd)：显示键盘按键或按键组合。
- [按钮组 (Button Group)](#button-group)：一组相关按钮，用于操作和拆分按钮。
- [输入组 (Input Group)](#input-group)：带图标、按钮、标签等的输入框组合。
- [Field](#field)：一个组件，覆盖你所有的表单需求。
- [Item](#item)：显示项目列表、卡片等。
- [Empty](#empty)：用于空状态显示。

### Spinner

先从最简单的开始：**Spinner** 和 **Kbd**。非常基础，大家都知道它们做什么。

这是渲染一个 Spinner 的方式：

```tsx
import { Spinner } from "@/components/ui/spinner"
```

```tsx
<Spinner />
```

它的样子是这样的：

<ComponentPreview
  name="spinner-basic"
  className="[&_.preview]:h-[250px] [&_pre]:!h-[250px]"
/>

在按钮中的效果：

<ComponentPreview
  name="spinner-button"
  className="[&_.preview]:h-[250px] [&_pre]:!h-[250px]"
/>

你可以编辑代码用自己的 Spinner 替换它。

<ComponentPreview
  name="spinner-custom"
  className="[&_.preview]:h-[250px] [&_pre]:!h-[250px]"
/>

### Kbd

Kbd 是用来渲染键盘按键的组件。

```tsx
import { Kbd, KbdGroup } from "@/components/ui/kbd"
```

```tsx
<Kbd>Ctrl</Kbd>
```

用 `KbdGroup` 来组合多个键盘按键。

```tsx showLineNumbers
<KbdGroup>
  <Kbd>Ctrl</Kbd>
  <Kbd>B</Kbd>
</KbdGroup>
```

<ComponentPreview
  name="kbd-demo"
  className="[&_.preview]:h-[250px] [&_pre]:!h-[250px]"
/>

你可以将它添加到按钮、工具提示、输入组等多种地方。

### 按钮组 (Button Group)

收到很多对此的需求：按钮组。它是一个容器，将相关按钮组合在一起，样式统一。很适合操作组、拆分按钮等场景。

<ComponentPreview
  name="button-group-demo"
  className="[&_.preview]:h-[250px] [&_pre]:!h-[250px]"
/>

代码如下：

```tsx
import { ButtonGroup } from "@/components/ui/button-group"
```

```tsx showLineNumbers
<ButtonGroup>
  <Button>按钮 1</Button>
  <Button>按钮 2</Button>
</ButtonGroup>
```

你可以嵌套按钮组，创建更复杂的布局并带有间距。

```tsx showLineNumbers
<ButtonGroup>
  <ButtonGroup>
    <Button>按钮 1</Button>
    <Button>按钮 2</Button>
  </ButtonGroup>
  <ButtonGroup>
    <Button>按钮 3</Button>
    <Button>按钮 4</Button>
  </ButtonGroup>
</ButtonGroup>
```

使用 `ButtonGroupSeparator` 创建拆分按钮，是经典的下拉模式。

<ComponentPreview
  name="button-group-dropdown"
  className="[&_.preview]:h-[250px] [&_pre]:!h-[250px]"
/>

也可以用来给输入框添加前缀或后缀按钮及文本。

<ComponentPreview
  name="button-group-select"
  className="[&_.preview]:h-[250px] [&_pre]:!h-[250px]"
/>

```tsx showLineNumbers
<ButtonGroup>
  <ButtonGroupText>前缀</ButtonGroupText>
  <Input placeholder="请输入内容..." />
  <Button>按钮</Button>
</ButtonGroup>
```

### 输入组 (Input Group)

Input Group 让你可以为输入框添加图标、按钮等等，就是那些你总想加在输入框边上的小东西。

```tsx
import {
  InputGroup,
  InputGroupAddon,
  InputGroupInput,
} from "@/components/ui/input-group"
```

```tsx showLineNumbers
<InputGroup>
  <InputGroupInput placeholder="搜索..." />
  <InputGroupAddon>
    <SearchIcon />
  </InputGroupAddon>
</InputGroup>
```

这是一个带图标的预览：

<ComponentPreview
  name="input-group-icon"
  className="[&_.preview]:h-[300px] [&_pre]:!h-[300px]"
/>

你也可以添加按钮：

<ComponentPreview
  name="input-group-button"
  className="[&_.preview]:h-[300px] [&_pre]:!h-[300px]"
/>

或者文字、标签、工具提示等：

<ComponentPreview
  name="input-group-text"
  className="[&_.preview]:h-[350px] [&_pre]:!h-[350px]"
/>

它同样适用于文本域，你能构建更复杂、具备各种调节项的表单，或者说是最新的 prompt 表单。

<ComponentPreview
  name="input-group-textarea"
  className="[&_.preview]:h-[450px] [&_pre]:!h-[450px]"
/>

下面是带有 Spinner 的酷炫样例：

<ComponentPreview
  name="input-group-spinner"
  className="[&_.preview]:h-[350px] [&_pre]:!h-[350px]"
/>

### Field

介绍一下 **Field**，这是用来构建非常复杂表单的组件。这种抽象非常优雅。

花了我很久时间才做对，它兼容所有表单库：Server Actions，React Hook Form，TanStack Form，Bring Your Own Form。

```tsx
import {
  Field,
  FieldDescription,
  FieldError,
  FieldLabel,
} from "@/components/ui/field"
```

这是一个带输入的基础字段：

```tsx showLineNumbers
<Field>
  <FieldLabel htmlFor="username">用户名</FieldLabel>
  <Input id="username" placeholder="Max Leiter" />
  <FieldDescription>
    为你的账号选择一个唯一的用户名。
  </FieldDescription>
</Field>
```

<ComponentPreview
  name="field-input"
  className="[&_.preview]:h-[350px] [&_pre]:!h-[350px]"
/>

它支持所有表单控件。输入框、文本域、选择框、复选框、单选框、开关、滑块，样样支持。完整示例：

<ComponentPreview
  name="field-demo"
  className="[&_.preview]:h-[850px] [&_pre]:!h-[850px]"
/>

下面是一些复选框字段：

<ComponentPreview
  name="field-checkbox"
  className="[&_.preview]:h-[500px] [&_pre]:!h-[500px]"
/>

你可以用 `FieldGroup` 和 `FieldSet` 来组合字段。非常适合多部分表单。

```tsx showLineNumbers
<FieldSet>
  <FieldLegend />
  <FieldGroup>
    <Field />
    <Field />
  </FieldGroup>
</FieldSet>
```

<ComponentPreview
  name="field-fieldset"
  className="[&_.preview]:h-[500px] [&_pre]:!h-[500px]"
/>

响应式布局很简单。用 `orientation="responsive"`，它会根据容器宽度自动切换垂直或水平布局。完成。

<ComponentPreview
  name="field-responsive"
  className="[&_.preview]:h-[600px] [&_pre]:!h-[600px]"
/>

再来一点新鲜的。给字段包裹 `FieldLabel`，轻松创建可选的字段组。非常简单，效果也很棒。

<ComponentPreview
  name="field-choice-card"
  className="[&_.preview]:h-[600px] [&_pre]:!h-[600px]"
/>

### Item

这是一个简单的 flex 容器，能容纳几乎任何类型的内容。

我做过无数次，决定封装成组件，现在我无时无刻不在用。用来显示项目列表、卡片等。

```tsx
import {
  Item,
  ItemContent,
  ItemDescription,
  ItemMedia,
  ItemTitle,
} from "@/components/ui/item"
```

一个基础的 Item：

```tsx showLineNumbers
<Item>
  <ItemMedia variant="icon">
    <HomeIcon />
  </ItemMedia>
  <ItemContent>
    <ItemTitle>仪表盘</ItemTitle>
    <ItemDescription>账号与活动总览。</ItemDescription>
  </ItemContent>
</Item>
```

<ComponentPreview
  name="item-demo"
  className="[&_.preview]:h-[300px] [&_.preview]:p-4 [&_pre]:!h-[300px]"
/>

你可以添加图标、头像或图片。

<ComponentPreview
  name="item-icon"
  className="[&_.preview]:h-[300px] [&_.preview]:p-4 [&_pre]:!h-[300px]"
/>

<ComponentPreview
  name="item-avatar"
  className="[&_.preview]:h-[300px] [&_.preview]:p-4 [&_pre]:!h-[300px]"
/>

使用 `ItemGroup` 显示项目列表：

<ComponentPreview
  name="item-group"
  className="[&_.preview]:h-[500px] [&_.preview]:p-4 [&_pre]:!h-[500px]"
/>

需要当做链接？用 `asChild` 属性：

```tsx showLineNumbers
<Item asChild>
  <a href="/dashboard">
    <ItemMedia variant="icon">
      <HomeIcon />
    </ItemMedia>
    <ItemContent>
      <ItemTitle>仪表盘</ItemTitle>
      <ItemDescription>账号与活动总览。</ItemDescription>
    </ItemContent>
  </a>
</Item>
```

<ComponentPreview
  name="item-link"
  className="[&_.preview]:h-[400px] [&_.preview]:p-4 [&_pre]:!h-[400px]"
/>

### Empty

最后一个：**Empty**。用于在应用中显示空状态。

```tsx
import {
  Empty,
  EmptyContent,
  EmptyDescription,
  EmptyMedia,
  EmptyTitle,
} from "@/components/ui/empty"
```

使用示例：

```tsx showLineNumbers
<Empty>
  <EmptyMedia variant="icon">
    <InboxIcon />
  </EmptyMedia>
  <EmptyTitle>暂无消息</EmptyTitle>
  <EmptyDescription>你还没有任何消息。</EmptyDescription>
  <EmptyContent>
    <Button>发送消息</Button>
  </EmptyContent>
</Empty>
```

<ComponentPreview
  name="empty-demo"
  className="[&_.preview]:h-[400px] [&_.preview]:p-4 [&_pre]:!h-[400px]"
/>

你也可以和头像搭配使用：

<ComponentPreview
  name="empty-avatar"
  className="[&_.preview]:h-[400px] [&_pre]:!h-[400px]"
/>

或者结合输入组，用于搜索结果或邮箱订阅：

<ComponentPreview
  name="empty-input-group"
  className="[&_.preview]:h-[450px] [&_pre]:!h-[450px]"
/>

就这些。七个新组件。兼容你所有的库。已经准备好投入你的项目。

---

## 2025年9月 - 注册表索引

我们创建了一个开源注册表索引，可以直接从中安装组件。

你可以搜索、查看和添加注册表索引中的项目，无需自行配置 `.components.json` 文件。

它们会自动添加到你的 `components.json` 中。

```bash
npx shadcn add @ai-elements/prompt-input
```

完整的注册表列表见：[https://ui.shadcn.com/r/registries.json](https://ui.shadcn.com/r/registries.json)。

要添加注册表到索引，请向 `shadcn/ui` 仓库提交 PR。详情见 [注册表索引文档](/docs/registry/registry-index)。

---

## 2025年8月 - shadcn CLI 3.0 与 MCP 服务器

我们刚刚发布了 shadcn CLI 3.0，支持命名空间注册表、先进认证、新命令以及重新设计的注册表引擎。

### 新特性

- [命名空间注册表](#namespaced-registries) - 支持通过 `@registry/name` 格式安装组件。
- [私有注册表](#private-registries) - 通过高级认证保障注册表安全。
- [搜索与发现](#search--discovery) - 新命令帮助你查找和预览代码。
- [MCP 服务器](#mcp-server) - 所有注册表的 MCP 服务器。
- [整体提速](#faster-everything) - 重新构建的注册表解析引擎。
- [改进错误处理](#improved-error-handling) - 提供更友好的错误提示给用户及 LLM。
- [升级指南](#upgrade-guide) - 现有用户迁移说明。

### 命名空间注册表

3.0 最大变化是支持命名空间注册表。你可以从社区注册表、公司私有注册表或内部注册表安装组件，格式是 `@registry/name`。

这让跨团队和跨项目分发代码更容易。

在 `components.json` 配置注册表：

```json title="components.json"
{
  "registries": {
    "@acme": "https://acme.com/r/{name}.json",
    "@internal": {
      "url": "https://registry.company.com/{name}",
      "headers": {
        "Authorization": "Bearer ${REGISTRY_TOKEN}"
      }
    }
  }
}
```

然后用 `@registry/name` 格式安装组件：

```bash
npx shadcn add @acme/button @internal/auth-system
```

完全去中心化，无中央注册机构。你可以自由创建命名空间，按团队需求组织组件。

```json title="components.json" showLineNumbers
{
  "registries": {
    "@design": "https://registry.company.com/design/{name}.json",
    "@engineering": "https://registry.company.com/eng/{name}.json",
    "@marketing": "https://registry.company.com/marketing/{name}.json"
  }
}
```

组件还可以依赖不同注册表的资源，解析及安装全部自动完成。

```json title="registry-item.json" showLineNumbers
{
  "name": "dashboard",
  "type": "registry:block",
  "registryDependencies": [
    "@shadcn/card", // 默认注册表
    "@v0/chart", // v0 注册表
    "@acme/data-table", // acme 注册表
    "@lib/data-fetcher", // 工具库
    "@ai/analytics-prompt" // AI prompt 资源
  ]
}
```

### 私有注册表

需要保密组件？没问题。支持用 token、API key、或自定义请求头来配置认证：

```json title="components.json"
{
  "registries": {
    "@private": {
      "url": "https://registry.company.com/{name}.json",
      "headers": {
        "Authorization": "Bearer ${REGISTRY_TOKEN}"
      }
    }
  }
}
```

私有组件保持私密，特别适合企业团队的专有 UI 库。

我们支持所有主流认证方式：Basic Auth、Bearer Token、API Key 查询参数、定制请求头。

详见 [认证文档](/docs/registry/authentication)。

### 搜索与发现

三条新命令让你轻松找到所需：

1. 预览注册表中的项目再安装

```bash
npx shadcn view @acme/auth-system
```

2. 在注册表中搜索项目

```bash
npx shadcn search @tweakcn -q "dark"
```

3. 列出某注册表的所有项目

```bash
npx shadcn list @acme
```

预览组件，跨多注册表搜索，提前查看代码和依赖。

### MCP 服务器

<Image
  src="/images/mcp.jpeg"
  width="1432"
  height="1050"
  alt="Lift Mode"
  className="mt-6 w-full overflow-hidden rounded-lg border"
/>

今年四月，我们[推出](https://x.com/shadcn/status/1917597228513853603)了 MCP 服务器的第一个版本。此后根据反馈打造了更完善的 MCP 服务器。

新特性：

- 支持所有注册表，零配置
- 一条命令集成到你喜爱的 MCP 客户端
- 优化底层工具
- CLI 和注册表集成更佳
- 同项目支持多个注册表

添加 MCP 服务器：

```bash
npx shadcn@latest mcp init
```

详见 [文档](/docs/mcp)。

### 整体提速

全部重写注册表解析引擎，速度更快，更智能，处理复杂依赖树更稳健。

- 依赖解析速度提升 3 倍
- 文件去重合并更智能
- 内置更好 monorepo 支持
- 给注册表作者更新的 `build` 命令

### 改进错误处理

注册表开发者现在可以提供自定义错误信息，帮助用户（与 LLM）定位问题。CLI 展示有用的操作建议：

```txt
未知注册表 "@acme"。确保在 components.json 中定义如下：
{
  "registries": {
    "@acme": "[REGISTRY_URL]"
  }
}
```

缺少环境变量时，CLI 会精准提示：

```txt
注册表 "@private" 需要以下环境变量：
  • REGISTRY_TOKEN

请将所需变量添加至 .env 或 .env.local 文件。
```

注册表作者可以在响应中加入自定义错误，帮助用户和 AI 快速理解并修复问题。

```txt
错误：
你无权访问 http://example.com/r/component 中的项。

消息：
[未授权] 你的 API key 已过期。请访问 https://example.com/api/renew-key 更新。
```

### 升级指南

最棒的是：没有破坏性变更。你现有的 `components.json` 和已安装组件照样可用。

开发者若用到程序化 API，部分函数已废弃，推荐用替代方案：

- `fetchRegistry` → `getRegistry`
- `resolveRegistryTree` → `resolveRegistryItems`
- schema 包从 `shadcn/registry` 移至 `shadcn/schema`

```diff
- import { registryItemSchema } from "shadcn/registry"
+ import { registryItemSchema } from "shadcn/schema"
```

就这样，其他一切皆正常工作。

---

## 2025年7月 - 通用注册表项

我们新增对通用注册表项的支持。可创建可分发给任何项目的注册表项，即无框架、无 components.json、无 Tailwind、无 React 依赖。

这个新类型开启了许多新工作流：你现在可以将代码、配置、规则、文档等分发到任何代码项目。

详见 [文档](/docs/registry/examples) 及示例。

---

## 2025年7月 - 本地文件支持

shadcn CLI 现在支持本地文件。可以用本地 JSON 文件初始化项目，也可添加组件、主题、hooks、工具等。

```bash
# 从本地文件初始化项目
npx shadcn init ./template.json

# 从本地文件添加组件
npx shadcn add ./block.json
```

此功能带来强大新工作流：

- **零配置** - 无需远程注册表
- **更快开发** - 本地测试注册表项再发布
- **强化代理与 MCP 工作流** - 本地生成并运行
- **私有组件** - 专有组件本地私密保存

---

## 2025年6月 - `radix-ui`

我们新增命令支持迁移到新版 `radix-ui` 包。该命令会将所有 `@radix-ui/react-*` 的导入替换为 `radix-ui`。

```bash
npx shadcn@latest migrate radix
```

它会自动更新所有 `ui` 组件里的导入，并安装 `radix-ui` 依赖。

```diff showLineNumbers title="components/ui/alert-dialog.tsx"
- import * as AlertDialogPrimitive from "@radix-ui/react-dialog"
+ import { AlertDialog as AlertDialogPrimitive } from "radix-ui"
```

命令执行后，请测试组件和项目。

**注意：** 若后续新增组件，要再次运行迁移命令更新导入。

## 2025年6月 - 日历组件

我们升级了 `Calendar` 组件，基于最新的 [React DayPicker](https://daypicker.dev) 版本。

这是一次重大升级，带来了众多新功能和改进。我们还构建了 30 多个日历区块，供你自由组合构建。

所有日历区块见 [区块库](/blocks/calendar)。

<Image src="/images/calendar-2.png" alt="日历" width={676} height={895} />

升级到最新版本，详见 [升级指南](/docs/components/calendar#upgrade-guide)。

## 2025年5月 - 新网站

我们将 [ui.shadcn.com](https://ui.shadcn.com) 升级到 Next.js 15.3 和 Tailwind v4，站点使用升级版的 `new-york` 组件。

同时略微调整设计，提升速度和导航顺畅度。

此升级为我们后续众多新特性铺路，敬请期待。

## 2025年4月 - MCP

我们正在为 shadcn/ui 注册表开发零配置 MCP 支持。只需一条命令 `npx shadcn registry:mcp` 即可使任意注册表兼容 MCP。

<Image
  src="/images/mcp.jpeg"
  width="1432"
  height="1050"
  alt="Lift Mode"
  className="mt-6 w-full overflow-hidden rounded-lg border"
/>

更多信息见[推文](https://x.com/shadcn/status/1917597228513853603)。

## 2025年3月 - shadcn 2.5.0

本周发布了 shadcn 2.5.0，带来一个很酷的功能：**任意位置解析**。

注册表现在可以将文件放在应用任意位置，并能正确解析导入，不必遵循固定目录结构，甚至可以添加注册表自身外的文件。

安装时，我们追踪所有文件，执行多轮解析，准确处理导入和别名，速度依然快。

## 2025年3月 - 跨框架路由支持

shadcn CLI 现在能自动检测你的框架，并自动适配路由。

支持所有框架，包括 Laravel、Vite 和 React Router。

## 2025年2月 - Tailwind v4

发布了 Tailwind v4 和 React 19 的首个预览版本，你可以立即尝试。

新特性：

- CLI 支持用 Tailwind v4 初始化项目
- 完整支持新 `@theme` 指令及行内选项
- 所有组件升级兼容 Tailwind v4 和 React 19
- 移除 forwardRefs，调整类型定义
- 每个原语组件新增 `data-slot` 属性用于样式
- 组件样式修正优化
- 吐司组件不再维护，推荐使用 Sonner
- 按钮默认使用默认鼠标指针
- 弃用默认样式，新项目使用 new-york 风格
- HSL 颜色转换为 OKLCH

详见 [文档](/docs/tailwind-v4)。

## 2025年2月 - 更新注册表 Schema

更新了注册表 Schema 以支持更多功能。

现在可定义扁平 JSON 格式的代码，通过 CLI 分发。

- 自定义样式：支持自带设计系统、组件和变量
- 可扩展、覆盖、组合第三方注册表和 LLM 的组件
- 安装主题、CSS 变量、hooks、动画，Tailwind 图层与工具类

## 2025年1月 - 区块

我们邀请社区参与区块库建设。分享你的组件和区块，帮助打造高质量复用组件库。

欢迎各种类型的区块：应用、营销、产品等。

详见 [文档](/docs/blocks)。

## 2024年12月 - Monorepo 支持

以前，在 monorepo 中使用 shadcn/ui 稍显麻烦，CLI 可以添加组件，但需手动管理安装路径和修正导入路径。

CLI 新增 monorepo 支持后，安装组件、依赖及注册表依赖会自动正确放置路径，同时自动处理导入。

详见 [文档](/docs/monorepo)。

## 2024年11月 - 图标

图标更新。new-york 风格默认改用 Lucide 图标集。

- 新项目默认使用 Lucide
- 现有项目无破坏性变更
- 可通过 CLI（可选）迁移原语到 Lucide

更多信息及背景详见 [推文](https://x.com/shadcn/status/1853902179041702169)。

## 2024年10月 - React 19

shadcn/ui 现已兼容 React 19 和 Next.js 15。

我们发布了升级指南助你迁移。

详见[此处](/docs/react-19)。

## 2024年10月 - 侧边栏

推出了 sidebar.tsx：25 个组件帮助你打造各种侧边栏。

我不喜欢重写侧边栏，因此已经构建了 30+ 种，覆盖所有类型，并简化核心成 sidebar.tsx，作为稳固基础。

它兼容 Next.js、Remix、Vite 和 Laravel。

查看公告 [这里](https://x.com/shadcn/status/1847359896557408461)。

## 2024年8月 - npx shadcn init

全新 CLI 现已发布，完全重写，带来诸多新功能和优化。你可以用 `npx shadcn add` 安装组件、主题、hooks、工具等。

这是迈向让你和你的 LLMs 可访问使用代码分发的重要一步。

主要特性：

1. CLI 支持所有主流 React 框架，包含 Next.js、Remix、Vite、Laravel。初始化新应用时，更新 Tailwind 文件而非覆盖。
2. 组件自带依赖。例如手风琴可定义 Tailwind 动画关键帧，添加到项目时自动更新 tailwind.config.ts。
3. 支持用 URL 远程添加组件。例如 `npx shadcn add https://acme.com/registry/navbar.json`。
4. 改进 init 命令，支持框架检测甚至一键初始化全新 Next.js 应用。
5. 创建新 Schema，支持你的组件注册表，允许通过 URL 分发私有组件。
6. 其他提升，如更好错误处理和 monorepo 支持。

今天就试试新 CLI：

```bash
npx shadcn init sidebar-01 login-01
```

### 更新你的项目

更新现有项目用新 CLI，需在 `components.json` 添加别名配置，覆盖 **components**、**utils**、**ui**、**lib**、**hooks**。

```json showLineNumbers {7-13} title="components.json"
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "tailwind": {
    // ...
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  }
}
```

若你用不同别名前缀，如 `~`，请将 `@` 替换。

## 2024年4月 - 引入 Lift Mode

推出了 [Blocks](/blocks) 里的新模式 **Lift Mode**。

启用 Lift Mode 后，可自动“提取”区块模板里的小组件，方便复制粘贴。

<a href="/blocks">
  <Image
    src="/images/lift-mode-light.png"
    width="1432"
    height="1050"
    alt="Lift Mode"
    className="mt-6 w-full overflow-hidden rounded-lg border dark:hidden"
  />
  <Image
    src="/images/lift-mode-dark.png"
    width="1432"
    height="1069"
    alt="Lift Mode"
    className="mt-6 hidden w-full overflow-hidden rounded-lg border shadow-sm dark:block"
  />
  <span className="sr-only">查看区块库</span>
</a>

借助 Lift Mode，你能直接复制区块模板里的卡片、按钮、表单等小组件，粘贴进你的项目。

访问 [Blocks](/blocks) 试用。

## 2024年3月 - 引入 Blocks

自发布以来，最大需求之一是布局：包含侧边栏的管理面板、营销页面区块、卡片等。

**现在发布 [Blocks](/blocks)**。

<a href="/blocks">
  <Image
    src="/images/dashboard-1.jpg"
    width="716"
    height="430"
    alt="管理面板"
    className="mt-6 w-full overflow-hidden rounded-lg border dark:hidden"
  />
  <Image
    src="/images/dashboard-1-dark.jpg"
    width="716"
    height="430"
    alt="管理面板"
    className="mt-6 hidden w-full overflow-hidden rounded-lg border shadow-sm dark:block"
  />
  <span className="sr-only">查看区块库</span>
</a>

Blocks 是可即用组件，用于快速构建应用。它们响应式、无障碍、可组合，遵循 shadcn/ui 的设计原则。

首批涵盖仪表盘布局和认证页面，后续会增加更多内容。

### 开源

Blocks 是开源的。GitHub 上可查源码。可在项目中使用、定制并贡献。

<a href="/blocks">
  <Image
    src="/images/dashboard-2.jpg"
    width="716"
    height="420"
    alt="AI 游乐场"
    className="mt-6 w-full overflow-hidden rounded-lg border dark:hidden"
  />
  <Image
    src="/images/dashboard-2-dark.jpg"
    width="716"
    height="420"
    alt="AI 游乐场"
    className="mt-6 hidden w-full overflow-hidden rounded-lg border shadow-sm dark:block"
  />
  <span className="sr-only">查看区块库</span>
</a>

### 请求区块

还新增“请求区块”功能。想要某个特定区块？在 GitHub 提交请求，社区可投票支持并开发。

<a href="/blocks">
  <Image
    src="/images/dashboard-3.jpg"
    width="716"
    height="420"
    alt="设置页面"
    className="mt-6 w-full overflow-hidden rounded-lg border dark:hidden"
  />
  <Image
    src="/images/dashboard-3-dark.jpg"
    width="716"
    height="420"
    alt="设置页面"
    className="mt-6 hidden w-full overflow-hidden rounded-lg border shadow-sm dark:block"
  />
  <span className="sr-only">查看区块库</span>
</a>

### v0

如果你有 [v0](https://v0.dev) 账号，可以用 **Edit in v0** 功能打开代码，用于 prompt 和进一步生成。

<div className="bg-background mt-6 flex aspect-video w-full items-center justify-center overflow-hidden rounded-lg border shadow-sm">
  <svg
    viewBox="0 0 40 20"
    fill="none"
    xmlns="http://www.w3.org/2000/svg"
    className="text-foreground h-40 w-40"
  >
    <path
      d="M23.3919 0H32.9188C36.7819 0 39.9136 3.13165 39.9136 6.99475V16.0805H36.0006V6.99475C36.0006 6.90167 35.9969 6.80925 35.9898 6.71766L26.4628 16.079C26.4949 16.08 26.5272 16.0805 26.5595 16.0805H36.0006V19.7762H26.5595C22.6964 19.7762 19.4788 16.6139 19.4788 12.7508V3.68923H23.3919V12.7508C23.3919 12.9253 23.4054 13.0977 23.4316 13.2668L33.1682 3.6995C33.0861 3.6927 33.003 3.68923 32.9188 3.68923H23.3919V0Z"
      fill="currentColor"
    ></path>
    <path
      d="M13.7688 19.0956L0 3.68759H5.53933L13.6231 12.7337V3.68759H17.7535V17.5746C17.7535 19.6705 15.1654 20.6584 13.7688 19.0956Z"
      fill="currentColor"
    ></path>
  </svg>
</div>

就这样。_期待看到你用 Blocks 建的作品_。

## 2024年3月 - 面包屑和输入 OTP

新增面包屑组件和输入 OTP 组件。

### 面包屑

一个可访问且灵活的面包屑组件，支持折叠项目、自定义分隔符、自带路由 `<Link />`，且能与 shadcn/ui 其他组件组合。

<ComponentPreview name="breadcrumb-demo" />

[查看更多示例](/docs/components/breadcrumb)

### 输入 OTP

一个功能完善的输入 OTP 组件，支持数字和字母验证码、长度自定义、复制粘贴，并符合无障碍标准。基于 [input-otp](https://github.com/guilhermerodz/input-otp)（作者 [@guilherme_rodz](https://twitter.com/guilherme_rodz)）构建。

<ComponentPreview name="input-otp-demo" />

[阅读文档](/docs/components/input-otp)

如果你有 [v0](https://v0.dev)，新组件可用于生成。

## 2023年12月 - 新组件、CLI 等

新增组件及 CLI 重大改进。

概览：

- [**Carousel**](#carousel) - 带动画、手势和键盘支持的轮播组件。
- [**Drawer**](#drawer) - 移动端出色的抽屉组件。
- [**Pagination**](#pagination) - 分页导航组件，含前后页按钮。
- [**Resizable**](#resizable) - 可调整大小的面板组组件和布局。
- [**Sonner**](#sonner) - 你唯一需要的吐司组件。
- [**CLI 更新**](#cli-updates) - 支持自定义 Tailwind 前缀和 `tailwind.config.ts`。

### Carousel

基于 [Embla Carousel](https://www.embla-carousel.com) 构建，功能丰富，支持无限循环、自动播放、垂直方向等。

<ComponentPreview name="carousel-demo" />

### Drawer

基于 [Vaul](https://github.com/emilkowalski/vaul)，由 [emilkowalski](https://twitter.com/emilkowalski) 开发。

试着在手机打开抽屉，特别棒！

<ComponentPreview name="drawer-demo" />

### Pagination

分页组件，导航灵活，支持你框架的 `<Link />`。

<ComponentPreview name="pagination-demo" />

### Resizable

用 `<Resizable />` 构建可调大小面板组和布局。

<ComponentPreview name="resizable-demo-with-handle" />

基于 [react-resizable-panels](https://github.com/bvaughn/react-resizable-panels)，支持鼠标、触摸和键盘。

### Sonner

另一款由 [emilkowalski](https://twitter.com/emilkowalski) 开发的终极吐司组件。现集成于 shadcn/ui。

<ComponentPreview name="sonner-demo" />

### CLI 更新

长久以来最受请求的特性之一。支持自定义 Tailwind 前缀，CLI 自动添加你的实用类前缀。

因此你可轻松在 Docusaurus、Nextra 等既有项目中使用 shadcn/ui，且无冲突。🔥

```tsx /tw-/
<AlertDialog className="tw-grid tw-gap-4 tw-border tw-bg-background tw-shadow-lg" />
```

兼容 `cn`、`cva` 和 CSS 变量。

CLI 也能自动识别 `tailwind.config.ts` 并添加 TypeScript 版本配置。

就这些。节日快乐。

## 2023年7月 - JavaScript

本项目和组件都用 TypeScript 编写。**建议你的项目也用 TS。**

不过，我们提供了 JS 版本组件，通过 [CLI](/docs/cli) 可用。

```txt
要使用 TypeScript（推荐）吗？ no
```

若选择不用 TS，可在 `components.json` 设置 `tsx` 标志。

```json {10} title="components.json" showLineNumbers
{
  "style": "default",
  "tailwind": {
    "config": "tailwind.config.js",
    "css": "src/app/globals.css",
    "baseColor": "zinc",
    "cssVariables": true
  },
  "rsc": false,
  "tsx": false,
  "aliases": {
    "utils": "~/lib/utils",
    "components": "~/components"
  }
}
```

可用如下 `jsconfig.json` 配置导入别名：

```json {4} title="jsconfig.json" showLineNumbers
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

## 2023年6月 - 新 CLI、样式等

今天带来多项更新：

- [**新 CLI**](#new-cli) - 完全重写，支持添加组件、依赖及配置导入路径。
- [**样式化**](#theming-with-css-variables-or-tailwind-colors) - 可选 CSS 变量或 Tailwind 颜色主题化。
- [**基础色**](#base-color) - 配置基础色，用于生成默认配色。
- [**React 服务器组件**](#react-server-components) - 可选择不使用 RSC，自动处理 `use client` 指令。
- [**样式概念**](#styles) - 新增 _Style_，带有自身组件、动画、图标等。
- [**退场动画**](#exit-animations) - 所有组件新增退场动画。
- [**其他更新**](#other-updates) - 新图标尺寸按钮，更新 sheet 组件等。
- [**项目更新方法**](#updating-your-project) - 如何手动更新项目。

---

### 新 CLI

过去几周开发全新 CLI，完全重写，增添诸多功能。

### `init`

```bash
npx shadcn@latest init
```

执行后，会让你回答几个问题，配置 `components.json`：

```txt showLineNumbers
Which style would you like to use? › Default
Which color would you like to use as base color? › Slate
Where is your global CSS file? › › app/globals.css
Do you want to use CSS variables for colors? › no / yes
Where is your tailwind.config.js located? › tailwind.config.js
Configure the import alias for components: › @/components
Configure the import alias for utils: › @/lib/utils
Are you using React Server Components? › no / yes
```

此文件包含组件信息：安装位置、导入路径、样式方案等。

配置此文件即可修改导入路径、设置基础色、切换样式方法。

```json title="components.json" showLineNumbers
{
  "style": "default",
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/app/globals.css",
    "baseColor": "zinc",
    "cssVariables": true
  },
  "rsc": false,
  "aliases": {
    "utils": "~/lib/utils",
    "components": "~/components"
  }
}
```

CLI 兼容各种目录结构，包括 `src`、`app`。

### `add`

```bash
npx shadcn@latest add
```

`add` 命令功能显著增强。可添加 UI 组件，也可导入更复杂组件（即将推出）。

CLI 会自动解析组件依赖，格式化，添加到项目。

### `diff`（实验功能）

```bash
npx shadcn diff
```

新增 `diff` 命令，帮你跟踪上游更新。

这条命令列出可更新组件：

```bash
npx shadcn diff
```

```txt
以下组件有更新：
- button
  - /path/to/my-app/components/ui/button.tsx
- toast
  - /path/to/my-app/components/ui/use-toast.ts
  - /path/to/my-app/components/ui/toaster.tsx
```

用 `diff [组件名]` 看更改内容：

```bash
npx shadcn diff alert
```

```diff /pl-12/
const alertVariants = cva(
- "relative w-full rounded-lg border",
+ "relative w-full pl-12 rounded-lg border"
)
```

---

### CSS 变量或 Tailwind 颜色主题化

你可以选择用 CSS 变量或 Tailwind 的实用类进行主题化。

添加组件时，CLI 会根据你在 `components.json` 配置自动选择主题化方式。

#### 实用类

```tsx /bg-zinc-950/ /text-zinc-50/ /dark:bg-white/ /dark:text-zinc-950/
<div className="bg-zinc-950 dark:bg-white" />
```

要使用实用类，`tailwind.cssVariables` 设为 `false`：

```json {6} title="components.json" showLineNumbers
{
  "tailwind": {
    "config": "tailwind.config.js",
    "css": "app/globals.css",
    "baseColor": "slate",
    "cssVariables": false
  }
}
```

#### CSS 变量

```tsx /bg-background/ /text-foreground/
<div className="bg-background text-foreground" />
```

要启用 CSS 变量，`tailwind.cssVariables` 设为 `true`：

```json {6} title="components.json" showLineNumbers
{
  "tailwind": {
    "config": "tailwind.config.js",
    "css": "app/globals.css",
    "baseColor": "slate",
    "cssVariables": true
  }
}
```

---

### 基础色

你可以配置基础色，用作组件默认配色。

```json {5} title="components.json" showLineNumbers
{
  "tailwind": {
    "config": "tailwind.config.js",
    "css": "app/globals.css",
    "baseColor": "zinc",
    "cssVariables": false
  }
}
```

可选值有 `gray`、`neutral`、`slate`、`stone`、`zinc`。

若启用 CSS 变量，会将基础色以变量形式写入 `globals.css`；若关，直接内联 Tailwind 实用类。

---

### React 服务器组件

若你用的框架不支持 React 服务器组件，可在 `components.json` 设 `rsc` 为 `false`，CLI 会自动加/删 `use client` 指令。

```json title="components.json" showLineNumbers
{
  "rsc": false
}
```

---

### 样式概念

介绍新概念 _Style_。

_它代表视觉基础：形状、图标、动画、排版等。_ 样式自带组件、动画、图标等。

当前提供两种样式：`default` 和 `new-york`（以后更多）。

<Image
  src="/images/style.jpg"
  width="716"
  height="402"
  alt="默认与 New York 风格"
  className="mt-6 overflow-hidden rounded-lg border"
/>

`default` 是你熟悉的风格，用 `lucide-react` 图标和 `tailwindcss-animate` 动画。

`new-york` 是新风格，按钮更小，卡片带阴影，图标用 [Radix Icons](https://icons.radix-ui.com)。

初始化时可选样式，保存在 `components.json`：

```json title="components.json" showLineNumbers
{
  "style": "new-york"
}
```

### 主题化

基于样式，再用 CSS 变量或 Tailwind 实用类主题化，能彻底改变组件视觉。

<Image
  src="/images/style-with-theming.jpg"
  width="716"
  height="402"
  alt="含主题化的样式"
  className="mt-6 overflow-hidden rounded-lg border"
/>

---

### 退场动画

所有组件新增退场动画。点击下方组合框，看细腻退场效果。

<ComponentPreview name="combobox-demo" className="[&_.preview]:items-start" />

动画支持用实用类自定义。

---

### 其他更新

#### 按钮

- 新增 `icon` 按钮尺寸

<ComponentPreview name="button-icon" />

#### Sheet

- `position` 改名为 `side`，与其他元素统一。

<ComponentPreview name="sheet-side" />

- 删除 `size` 属性，推荐用响应式 className 代替：`w-[200px] md:w-[450px]`。

---

### 更新你的项目

因为是复制粘贴模式，需手动更新项目获取最新改动。

<Callout className="mt-4">
  注意：我们正在开发 [`diff`](#diff-experimental) 命令帮你跟踪上游更新。
</Callout>

<Steps>

### 添加 `components.json`

项目根目录建 `components.json`：

```json title="components.json" showLineNumbers
{
  "style": "default",
  "rsc": true,
  "tailwind": {
    "config": "tailwind.config.js",
    "css": "app/globals.css",
    "baseColor": "slate",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils"
  }
}
```

根据你项目结构调整 `tailwind.css` 和别名。

### 按钮

给 `buttonVariants` 添加 `icon` 尺寸：

```tsx {7} title="components/ui/button.tsx" showLineNumbers
const buttonVariants = cva({
  variants: {
    size: {
      default: "h-10 px-4 py-2",
      sm: "h-9 rounded-md px-3",
      lg: "h-11 rounded-md px-8",
      icon: "h-10 w-10",
    },
  },
})
```

### Sheet

1. 用以下代码替换 `sheet.tsx` 内容（详见文档中完整代码段）。

2. 替换 `position` 为 `side`：

```diff /position/ /side/
- <Sheet position="right" />
+ <Sheet side="right" />
```

</Steps>

### 感谢

感谢所有使用、反馈和贡献本项目的朋友们，感谢🙏
