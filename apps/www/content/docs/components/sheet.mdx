---
title: 抽屉
description: 扩展 Dialog 组件以显示与屏幕主内容互补的内容。
component: true
links:
  doc: https://www.radix-ui.com/docs/primitives/components/dialog
  api: https://www.radix-ui.com/docs/primitives/components/dialog#api-reference
---

<ComponentPreview name="sheet-demo" description="一个抽屉组件。" />

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add sheet
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>安装以下依赖：</Step>

```bash
npm install @radix-ui/react-dialog
```

<Step>将以下代码复制并粘贴到您的项目中。</Step>

<ComponentSource name="sheet" />

<Step>更新导入路径以匹配您的项目设置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

### 用法

```tsx
import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet"
```

```tsx
<Sheet>
  <SheetTrigger>打开</SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>您确定吗？</SheetTitle>
      <SheetDescription>
        此操作无法撤消。这将永久删除您的帐户并从我们的服务器中移除您的数据。
      </SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
```

## 示例

### 侧边

使用 `side` 属性来 `<SheetContent />` 指示组件将出现在屏幕的哪个边缘。值可以是 `top`、`right`、`bottom` 或 `left`。

<ComponentPreview name="sheet-side" />

### 尺寸

您可以使用 CSS 类调整抽屉的大小：

```tsx {3}
<Sheet>
  <SheetTrigger>打开</SheetTrigger>
  <SheetContent className="w-[400px] sm:w-[540px]">
    <SheetHeader>
      <SheetTitle>您确定吗？</SheetTitle>
      <SheetDescription>
        此操作无法撤消。这将永久删除您的帐户并从我们的服务器中移除您的数据。
      </SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
```