---
title: 按钮
description: 显示一个按钮或一个看起来像按钮的组件。
featured: true
component: true
---

<ComponentPreview name="button-demo" description="一个按钮" />

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add button
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>安装以下依赖：</Step>

```bash
npm install @radix-ui/react-slot
```

<Step>将以下代码复制并粘贴到你的项目中。</Step>

<ComponentSource name="button" />

<Step>更新导入路径以匹配你的项目设置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx
import { Button } from "@/components/ui/button"
```

```tsx
<Button variant="outline">按钮</Button>
```

## 链接

你可以使用 `buttonVariants` 辅助函数创建一个看起来像按钮的链接。

```tsx
import { buttonVariants } from "@/components/ui/button"
```

```tsx
<Link className={buttonVariants({ variant: "outline" })}>点击这里</Link>
```

或者，你可以设置 `asChild` 参数并嵌套链接组件。

```tsx
<Button asChild>
  <Link href="/login">登录</Link>
</Button>
```

## 示例

### 主要

<ComponentPreview name="button-demo" description="一个主要按钮" />

### 次要

<ComponentPreview name="button-secondary" description="一个次要按钮" />

### 破坏性

<ComponentPreview
  name="button-destructive"
  description="一个破坏性按钮"
/>

### 轮廓

<ComponentPreview
  name="button-outline"
  description="一个使用轮廓变体的按钮."
/>

### 幽灵

<ComponentPreview
  name="button-ghost"
  description="一个使用幽灵变体的按钮"
/>

### 链接

<ComponentPreview
  name="button-link"
  description="一个使用链接变体的按钮."
/>

### 图标

<ComponentPreview name="button-icon" description="一个图标按钮" />

### 带图标

<ComponentPreview name="button-with-icon" description="一个带图标的按钮" />

### 加载中

<ComponentPreview
  name="button-loading"
  description="一个具有加载状态的按钮."
/>

### 作为子组件

<ComponentPreview
  name="button-as-child"
  description="一个包装自定义链接组件的按钮"
/>

## 更新日志

### 2024-10-16 图标的类

已将 `gap-2 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0` 添加到按钮中，以便自动设置按钮内图标的样式。

在你的 `button.tsx` 文件中的 `cva` 调用中添加以下类。

```diff showLineNumbers title="button.tsx"
const buttonVariants = cva(
  "inline-flex ... gap-2 [&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0"
)
```