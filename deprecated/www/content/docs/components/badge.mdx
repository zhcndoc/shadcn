---
title: 徽章
description: 显示徽章或看起来像徽章的组件。
component: true
---

<ComponentPreview
  name="badge-demo"
  title="徽章"
  description="一个默认的徽章"
/>

## 安装

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">手动</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add badge
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>将以下代码复制并粘贴到您的项目中。</Step>

<ComponentSource name="badge" />

<Step>更新导入路径以匹配您的项目设置。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 用法

```tsx
import { Badge } from "@/components/ui/badge"
```

```tsx
<Badge variant="outline">徽章</Badge>
```

### 链接

您可以使用 `badgeVariants` 辅助函数创建一个看起来像徽章的链接。

```tsx
import { badgeVariants } from "@/components/ui/badge"
```

```tsx
<Link className={badgeVariants({ variant: "outline" })}>徽章</Link>
```

## 示例

### 默认

<ComponentPreview
  name="badge-demo"
  title="徽章"
  description="一个默认的徽章"
/>

---

### 次要

<ComponentPreview
  name="badge-secondary"
  title="徽章"
  description="使用次要作为变体的徽章."
/>

---

### 轮廓

<ComponentPreview
  name="badge-outline"
  title="徽章"
  description="使用轮廓作为变体的徽章."
/>

---

### 破坏性

<ComponentPreview name="badge-destructive" description="一个破坏性徽章." />
