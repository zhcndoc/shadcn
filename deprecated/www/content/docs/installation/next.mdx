---
title: Next.js
description: 安装和配置 shadcn/ui 以支持 Next.js。
---

<Callout className="bg-blue-50 border-blue-600 dark:border-blue-900 dark:bg-blue-950 mb-6 [&_code]:bg-blue-100 dark:[&_code]:bg-blue-900">
  **注意：** 以下指南适用于 Tailwind v4。如果您使用的是 Tailwind v3，请使用 `shadcn@2.3.0`。
</Callout>

<Steps>

### 创建项目

运行 `init` 命令以创建一个新的 Next.js 项目或设置一个现有项目：

```bash
npx shadcn@latest init
```

选择 Next.js 项目或 Monorepo。

### 添加组件

现在您可以开始向项目中添加组件。

```bash
npx shadcn@latest add button
```

上面的命令将把 `Button` 组件添加到您的项目中。然后您可以像这样导入它：

```tsx {1,6} showLineNumbers
import { Button } from "@/components/ui/button"

export default function Home() {
  return (
    <div>
      <Button>点击我</Button>
    </div>
  )
}
```

</Steps>